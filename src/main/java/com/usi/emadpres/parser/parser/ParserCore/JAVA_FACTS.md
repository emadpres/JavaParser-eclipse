# Important Java Syntax Facts Related to Parser

1. Constructor can have no name for anonymous types:
```
    Thread t = new Thread() { public void run(){} };
```
2. Method name can contain `$` 
```
    String $$(int)
```
